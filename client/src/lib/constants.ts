/**
 * Shared client-side constants.
 * Single source of truth for values repeated across pages and components.
 */

/** Money-back guarantee window used across Pricing, Landing, modals, SEO copy. */
export const GUARANTEE_DAYS = 30;
